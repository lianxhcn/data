# 数据字典

本项目使用固定随机种子生成的教学模拟数据，不来自真实调查。

## 节点表

- `student_id`：匿名学生编号 `S01`-`S30`。
- `grade`：博士学习年级，取值 1-4。
- `field`：模拟研究方向。

## 边表

- `source`：发起求助者。
- `target`：被求助者。
- `weight`：一个月内求助次数，表示联系强度。

`S29` 和 `S30` 被有意设置为孤立节点，用来检验程序是否从节点表保留全部观察对象。
