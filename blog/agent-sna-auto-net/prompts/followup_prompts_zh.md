# 实际后续追问

## 1. 图形筛选

当前网络图较密。计算指标继续使用完整网络，但用于展示的图形只绘制 `weight >= 2` 的边。请在图注中明确区分分析网络和展示子图，不要让读者误以为指标是在删边后计算的。

## 2. 图形调整

请调整图形：节点大小表示加权入度，节点颜色表示社群，边宽表示联系频次；突出标注加权入度最高的节点和中介中心性最高的节点。中文字号不得小于小五，图例不得遮挡节点。

## 3. Stata 导出

请将节点指标导出为 Stata 19 可以读取的文件，并编写一个简短 do-file，演示如何读取数据、查看变量标签并按照加权入度排序。不要在 do-file 中写入本机绝对路径。
